import { createSlice } from "@reduxjs/toolkit";

const troikaSlice = createSlice({
  name: "model",
  initialState: {},
  reducers: {},
});

export const {} = troikaSlice.actions;

export default troikaSlice.reducer;
